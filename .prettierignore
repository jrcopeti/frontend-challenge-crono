dist
coverage
pnpm-lock.yaml
src/assets/figma

# `claude-code-action` refuses to run when a PR's copy of its workflow differs
# from the default branch's by even one byte, so Prettier must not touch these.
.github/workflows/claude-code-review.yml
.github/workflows/claude.yml
